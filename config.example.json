{
  "_comment": "img-to-latex-pdf configuration — copy to config.json and customize",
  "_docs": "https://github.com/QwenLM/Qwen2.5-VL",

  "backend": "auto",
  "model": "qwen3-vl:2b",
  "timeout": 300,

  "backends": {
    "ollama": {
      "_comment": "Ollama local server — easiest setup: https://ollama.com",
      "model": "qwen3-vl:2b",
      "host": "http://localhost:11434"
    },

    "openai_compatible": {
      "_comment": "OpenAI-compatible API — works with vLLM, SGLang, llama.cpp server, etc.",
      "_vllm_example": "python -m vllm.entrypoints.openai.api_server --model Qwen/Qwen2.5-VL-7B-Instruct",
      "_sglang_example": "python -m sglang.launch_server --model Qwen/Qwen2.5-VL-7B-Instruct",
      "model": "qwen3-vl-8b",
      "base_url": "http://localhost:8000/v1",
      "api_key": "not-needed"
    },

    "transformers": {
      "_comment": "Direct HuggingFace transformers — needs GPU for reasonable speed",
      "_install": "pip install transformers torch pillow accelerate",
      "model": "Qwen/Qwen2.5-VL-7B-Instruct",
      "device": "auto",
      "torch_dtype": "auto"
    }
  },

  "latex": {
    "compiler": "xelatex",
    "_comment_compiler": "xelatex is required for CJK (Chinese/Japanese/Korean) via ctex",
    "default_class": "ctexart",
    "_alternatives": {
      "english": "article",
      "slides": "beamer",
      "formula_only": "standalone"
    },
    "output_dir": "desktop",
    "cleanup_aux": true
  },

  "prompts": {
    "_comment": "Default OCR prompts per language and mode — can be overridden per-call",
    "zh": "请仔细识别这张图片中的所有文字内容，包括题目、公式、选项等。一字不漏地输出图片中的全部文字。",
    "zh_retry": "请再次仔细识别图片中的所有文字，包括题目编号、公式、条件和问题。不要遗漏任何内容。",
    "en": "Please carefully recognize all text content in this image, including titles, formulas, tables, and options. Output every word exactly as it appears.",
    "en_retry": "Please re-examine the image and extract all text, including labels, equations, conditions, and questions. Do not omit any content.",
    "math": "Extract all mathematical content from this image. For formulas, use LaTeX notation ($...$ inline, $$...$$ display). Transcribe all text exactly.",
    "table": "Extract the table from this image. Output as a pipe-delimited markdown table. Preserve all cell content exactly.",
    "general": "Extract and transcribe all visible text from this image. Preserve the original structure."
  },

  "model_recommendations": {
    "_comment": "Recommended models by use case and hardware",
    "quick_ocr": {
      "model": "qwen3-vl:2b",
      "ram": "~2 GB",
      "speed": "fast (60-180s on CPU)",
      "use_case": "Simple text OCR, math problems"
    },
    "balanced": {
      "model": "qwen3-vl:8b",
      "ram": "~6 GB",
      "speed": "moderate",
      "use_case": "Complex layouts, tables, detailed math"
    },
    "accurate": {
      "model": "qwen3-vl:32b",
      "ram": "~20 GB",
      "speed": "slow (GPU recommended)",
      "use_case": "Dense documents, multilingual, precision-critical"
    },
    "best": {
      "model": "qwen3-vl:72b",
      "ram": "~40 GB",
      "speed": "slowest (GPU required)",
      "use_case": "Maximum accuracy, academic publishing"
    }
  }
}
